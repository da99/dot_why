#!/usr/bin/env ruby

#!/usr/bin/env bash
#!/usr/bin/env lua
#
#set -u -e -o pipefail

require("dot_why")

mod  = ARGV[0]
file = ARGV[1]

require "#{mod}"


page = Dot_Why::Template.new(file)
puts(page.to_html(:prettyprint=>true))

