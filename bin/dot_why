#!/usr/bin/env ruby

#!/usr/bin/env bash
#!/usr/bin/env lua
#
#set -u -e -o pipefail

require("dot_why")

args = ARGV.dup
file = args.pop()
mods = args

mods.each do |m|
  require "#{m}"
end


page = Dot_Why::Template.new(file)
puts(page.to_html(:prettyprint=>true))

